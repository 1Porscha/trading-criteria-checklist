// // bring in mongoose
// const mongoose = require("mongoose");

// const completedTradeSchema = new mongoose.Schema({
//     // look at cafe app item schema to see how to put ids in other models
//     // completedTrades: ["put trade IDs here"]
//     completedTrade: {type: mongoose.Schema.Types.ObjectId, ref: 'completedTrade'}
// });

// const completedTrade = mongoose.model('CompletedTrade', completedTradeSchema)

// module.exports = completedTrade;

